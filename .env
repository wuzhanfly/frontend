# Blockscout 本地开发环境配置文件 (.env.local)
# 根据提供的配置项生成

# ==================== 应用基础配置 (必需) ====================
NEXT_PUBLIC_APP_HOST=testnet.maiscan.org
NEXT_PUBLIC_APP_PROTOCOL=https
# 应用端口 (默认 3000)
NEXT_PUBLIC_APP_PORT=3000
# 应用环境
NEXT_PUBLIC_APP_ENV=development
# 是否使用 Next.js 代理
NEXT_PUBLIC_USE_NEXT_JS_PROXY=false

# ==================== API 配置 (必需) ====================
NEXT_PUBLIC_API_HOST=testnet.maiscan.org
NEXT_PUBLIC_API_PROTOCOL=https
NEXT_PUBLIC_API_BASE_PATH=/
NEXT_PUBLIC_API_WEBSOCKET_PROTOCOL=ws
# API 端口 (如果与主机不同)
# NEXT_PUBLIC_API_PORT=80

# ==================== 区块链参数 (必需) ====================
NEXT_PUBLIC_NETWORK_NAME=zkStack BNB Chain
NEXT_PUBLIC_NETWORK_SHORT_NAME=zkBNB
NEXT_PUBLIC_NETWORK_ID=9720
NEXT_PUBLIC_NETWORK_CURRENCY_NAME=BNB
NEXT_PUBLIC_NETWORK_CURRENCY_SYMBOL=BNB
NEXT_PUBLIC_NETWORK_CURRENCY_DECIMALS=18
NEXT_PUBLIC_NETWORK_RPC_URL=http://13.212.114.138:10575
NEXT_PUBLIC_IS_TESTNET=true

# ==================== Rollup 配置 (zkSync Era) ====================
NEXT_PUBLIC_ROLLUP_TYPE=zkSync
NEXT_PUBLIC_ROLLUP_L1_BASE_URL=https://testnet.bscscan.com
# 如果需要指定父链信息 (更现代的方式)，可以使用以下配置:
NEXT_PUBLIC_ROLLUP_PARENT_CHAIN={'id': 97, 'name': 'BSC Testnet', 'baseUrl': 'https://testnet.bscscan.com', 'rpcUrls': ['https://data-seed-prebsc-1-s1.bnbchain.org:8545'], 'currency': {'name': 'BNB', 'symbol': 'BNB', 'decimals': 18}, 'isTestnet': true}

# ==================== UI 配置 ====================
NEXT_PUBLIC_HOMEPAGE_CHARTS=['daily_txs']
NEXT_PUBLIC_HOMEPAGE_STATS=['latest_batch','average_block_time','total_txs','wallet_addresses','gas_tracker']
# 导航栏布局
NEXT_PUBLIC_NAVIGATION_LAYOUT=vertical

# ==================== 微服务配置 ====================
NEXT_PUBLIC_STATS_API_HOST=https://testnet.maiscan.org
NEXT_PUBLIC_VISUALIZE_API_HOST=https://testnet.maiscan.org
# 如果这些微服务有基础路径，可以添加:
# NEXT_PUBLIC_STATS_API_BASE_PATH=/
# NEXT_PUBLIC_VISUALIZE_API_BASE_PATH=/

# ==================== 功能开关 ====================
NEXT_PUBLIC_IS_ACCOUNT_SUPPORTED=true
# API 文档规范 URL
NEXT_PUBLIC_API_SPEC_URL=https://raw.githubusercontent.com/blockscout/blockscout-api-v2-swagger/main/swagger.yaml
# 启用 Gas Tracker (默认启用)
NEXT_PUBLIC_GAS_TRACKER_ENABLED=true
# 启用 Marketplace (如果需要)
# NEXT_PUBLIC_MARKETPLACE_ENABLED=true

# ==================== 内部 API 配置 (用于服务端渲染) ====================
API_HOST=backend
# API 端口 (如果 backend 服务使用非标准端口)
# API_PORT=80

# ==================== 其他可选配置 ====================
# 网络 Logo 和 Icon (替换为实际 URL)
# NEXT_PUBLIC_NETWORK_LOGO=https://example.com/logo.svg
# NEXT_PUBLIC_NETWORK_ICON=https://example.com/icon.png

# ==================== 多链配置 (Multichain) ====================
# 为避免 "Module not found: Can't resolve 'fs'" 错误，
# 需要禁用多链聚合器功能或正确配置其 API 端点。
# 禁用多链功能 (推荐用于本地开发，除非明确需要)
NEXT_PUBLIC_MULTICHAIN_AGGREGATOR_API_HOST=
# 如果需要启用多链功能，请取消注释并设置正确的 API 地址和基础路径：
# NEXT_PUBLIC_MULTICHAIN_AGGREGATOR_API_HOST=http://your-multichain-api-host.com
# NEXT_PUBLIC_MULTICHAIN_AGGREGATOR_BASE_PATH=/

# ==================== Optimism Superchain 配置 ====================
# 为避免与多链配置相关的错误（如 'fs' 模块错误和 'map' 调用错误），
# 需要显式禁用 OP Superchain 功能。
NEXT_PUBLIC_OP_SUPERCHAIN_ENABLED=false